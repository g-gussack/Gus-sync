import { ado } from "./ado";
import { app } from "./app";
import { shell } from "./shell";
import { storage } from "./storage";
import { theme } from "./theme";
import { window } from "./window";

export const router = {
  ado,
  theme,
  window,
  app,
  shell,
  storage,
};
